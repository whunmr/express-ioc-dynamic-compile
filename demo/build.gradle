project (":demo") {
    dependencies {
        compile(project(':express-ioc'))
    }

    task(runDemo, dependsOn: 'classes', type: JavaExec) {
        main = 'com.dummy.Demo'
        classpath = sourceSets.main.runtimeClasspath
    }
    
    defaultTasks 'runDemo'
}
